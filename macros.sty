% 供参考，可删

%==================== 引用 ====================%
\newcommand{\sectionid}{}
\newcommand{\mlbl}[1]{\label{\sectionid-#1}}
\newcommand{\mref}[2][\sectionid]{\autoref{#1-#2}}


%==================== 数学 ====================%
\newcommand{\Sufficiency}{\noindent\textit{充分性.\ \ }}
\newcommand{\Necessity}{\noindent\textit{必要性.\ \ }}

\newcommand{\Prob}[1]{\textsc{#1}}
\newcommand{\Cls}[1]{\textsf{#1}}

\newcommand{\e}{\mathrm{e}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\rarr}{\rightarrow}
\newcommand{\Rarr}{\Rightarrow}
\DeclareMathOperator{\poly}{poly}

\usepackage{amsmath,amssymb}    % AMSLaTeX宏包 用来排出更加漂亮的公式
\makeatletter
\renewcommand\normalsize{%
 \abovedisplayskip 1\p@ \@plus1\p@ \@minus6\p@
 \abovedisplayshortskip \z@ \@plus3\p@
 \belowdisplayshortskip 3\p@ \@plus3\p@ \@minus3\p@
 \belowdisplayskip \abovedisplayskip
 \let\@listi\@listI}
\makeatother

\newcommand{\gG}{\mathcal{G}} % 图
\newcommand{\mC}{\mathbf{C}} % 矩阵 C
\newcommand{\mX}{\mathbf{X}} % 矩阵 X
\newcommand{\mY}{\mathbf{Y}} % 矩阵 Y
\newcommand{\vy}{\mathbf{y}} % 向量 y
\newcommand{\onehot}{\mathrm{onehot}} % onehot 函数
\newcommand{\argmax}{\mathop{\mathrm{argmax}}} % argmax 函数
\newcommand{\mI}{\mathbf{I}} % 定义单位矩阵符号

% 系统名称
\newcommand{\Mname}{GNNA}